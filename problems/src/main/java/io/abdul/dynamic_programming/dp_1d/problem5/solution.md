Similar to Max Sum of non-adjacent elements.
But catch is, how do we handle circular array case.
Simply find max sum from 0 to n-2 (excluding last) and 1 to n-1 (excluding first) and return max of them